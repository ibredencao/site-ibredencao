# Copie para .env e preencha (o .env NÃO vai para o git).
# Só é necessário quando/ se formos puxar os episódios do Spotify pela Web API
# (para exibir até 9 sermões do Spotify como cards, igual ao YouTube).
# Crie um app grátis em https://developer.spotify.com/dashboard e copie as chaves.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
